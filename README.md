# StudentGradeTracker


📌 Project Overview

Student Grade Tracker is a simple Java console-based application that helps users manage and analyze student grades. The program allows users to enter student names and grades, stores the data using an ArrayList, and generates a summary report containing the average, highest, and lowest scores.

This project demonstrates the use of core Java concepts such as classes, objects, ArrayLists, loops, conditional statements, and user input handling.

🚀 Features
Add multiple student records.
Store student names and grades using ArrayList.
Calculate the average score of all students.
Identify the highest-scoring student.
Identify the lowest-scoring student.
Display a detailed summary report.
Simple and user-friendly console interface.
🛠️ Technologies Used
Java
ArrayList Collection Framework
Object-Oriented Programming (OOP)
Scanner Class for User Input

⚙️ How It Works
The user enters the number of students.
The program collects each student's name and grade.
Student data is stored in an ArrayList.
The application calculates:
Average Score
Highest Score
Lowest Score
A summary report is displayed on the console.

🔮 Future Enhancements
Add grade categories (A, B, C, D, F).
Save student records to a file.
Implement search and update functionality.
Create a graphical user interface (GUI) using Java Swing or JavaFX.
Export reports in PDF or CSV format.
